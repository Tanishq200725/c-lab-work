//write a program to read only numbers in file and display the sum.assume the numbers are written with spaces
#include<stdio.h>
int main(){
file *fp;
char ch;
int sum=0,num=0;
fp=fopen("file67.txt","r");
if(fp==NULL){
    printf("file not found");
}
else{
    ch=fgetc(fp);
    while(ch!=EOF){
       

        }
    }
}
}